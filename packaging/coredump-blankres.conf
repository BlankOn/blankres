# blankres reads core dumps that systemd-coredump has already written, so they must be saved to
# disk rather than kept in the journal. Without this the daemon can still report a crash, but a
# full payload can never be collected.
[Coredump]
Storage=external
Compress=yes
